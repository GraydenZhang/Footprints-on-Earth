# Footprints on Earth PRD

文档日期：2026-05-16
产品版本定位：v0.1 本地足迹 CSV 可视化工具

## 1. 项目概述

Footprints on Earth 是一个本地运行的个人足迹可视化网页应用。用户可以加载默认演示 CSV，也可以选择或拖入自己的足迹 CSV，在 3D 地球或平面地图上查看位置点、轨迹线、时间范围、日期分组和单点明细。

产品核心价值是让用户在不上传私人位置数据的前提下，快速查看、筛选和回顾长期轨迹数据。项目当前以静态 Web 应用为主体，配套 Node.js 本地静态服务和 Windows 便携启动器。

## 2. 背景与问题

个人足迹数据通常包含高敏感位置、时间和运动状态信息。常见地图或数据分析工具要么需要上传数据，要么配置复杂，不适合非技术用户快速浏览自己的历史轨迹。

本项目要解决的问题：

- 用户需要在本机查看足迹数据，降低隐私泄露风险。
- 用户需要直观看到足迹点、轨迹线和跨时间的移动范围。
- 用户需要按年、月、日或任意时间范围快速定位某段足迹。
- 用户需要一个开箱即用的演示数据和便携运行方式。

## 3. 产品目标

### 3.1 当前版本目标

- 支持本地加载 CSV 足迹数据并完成可视化。
- 提供 3D 地球和平面地图两种查看模式。
- 支持 OpenStreetMap 默认图源，并允许配置高德地图图源。
- 支持按时间滑块、年、月、日筛选数据。
- 支持点、线、数据列表和地图视野联动。
- 确保个人 CSV 默认不被提交到仓库，用户数据仅保存在浏览器本地。

### 3.2 非目标

- 当前版本不做云端账号、登录、同步或多人协作。
- 当前版本不做服务端数据分析和长期托管。
- 当前版本不提供移动 App 原生客户端。
- 当前版本不提供轨迹编辑、纠偏、合并和导出报告能力。

## 4. 目标用户

| 用户类型 | 需求 | 典型场景 |
| --- | --- | --- |
| 普通个人用户 | 本地查看自己的足迹 CSV | 从手机、定位工具或备份数据中导出 CSV 后查看轨迹 |
| 旅行记录用户 | 回看跨城市、跨国家移动路径 | 查看某次旅行的日期、路线和停留点 |
| 开发/维护者 | 快速验证足迹数据展示效果 | 使用演示 CSV 调试地图、筛选、打包版本 |

## 5. 使用场景

### 5.1 首次打开演示数据

1. 用户启动本地服务或便携版程序。
2. 页面默认加载 `data/footprint_test_file.csv`。
3. 地图显示全部足迹点和轨迹线。
4. 用户可通过全览按钮恢复全局视野。

### 5.2 加载个人 CSV

1. 用户点击“选择 CSV”或将 CSV 文件拖入页面。
2. 系统解析 CSV，校验必需字段。
3. 系统跳过无效行并提示跳过数量。
4. 系统在地图、统计卡片、日期列表和数据点列表中展示新数据。
5. 浏览器通过 IndexedDB 保存上次选择的 CSV，下次打开时优先恢复。

### 5.3 按日期筛选查看

1. 用户打开“时间列表”。
2. 用户选择某一年、某月或某日。
3. 系统更新可见点、轨迹线、统计信息和地图视野。
4. 用户点击某个数据点，地图飞到该点并高亮。

### 5.4 切换地图设置

1. 用户打开设置菜单。
2. 用户可切换 3D 地球/平面地图。
3. 用户可切换 OpenStreetMap/高德地图图源。
4. 用户可调整点大小、轨迹粗细、是否显示点、是否显示线、是否连接远距离间隔轨迹。
5. 系统保存主要显示设置，下次打开自动恢复。

## 6. 功能需求

### 6.1 CSV 加载与解析

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| F-001 | 默认加载公开演示 CSV | P0 | 页面启动后自动读取 `data/footprint_test_file.csv` 并渲染地图 |
| F-002 | 支持手动选择 CSV | P0 | 用户点击“选择 CSV”后可加载本地 `.csv` 文件 |
| F-003 | 支持拖拽 CSV 到窗口 | P1 | 用户拖入 `.csv` 文件后触发同样的解析和渲染流程 |
| F-004 | 校验必需字段 | P0 | CSV 缺少 `dataTime`、`longitude`、`latitude` 任一列时显示错误 |
| F-005 | 跳过无效行 | P0 | 经纬度或时间无效的行不渲染，并在状态中提示跳过数量 |
| F-006 | 保存上次选择 CSV | P1 | 浏览器支持 IndexedDB 时，下次打开优先恢复上次文件内容 |
| F-007 | 恢复默认演示 | P1 | 点击恢复后清除上次 CSV，重载演示数据并重置显示设置 |

### 6.2 地图展示

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| F-101 | 使用 MapLibre GL JS 渲染地图 | P0 | 页面可显示可缩放、可拖拽的地图 |
| F-102 | 支持 3D 地球模式 | P0 | 地图投影为 globe，默认进入 3D 地球视图 |
| F-103 | 支持平面地图模式 | P1 | 用户切换后地图投影为 mercator |
| F-104 | 支持 OpenStreetMap 图源 | P0 | 默认地图瓦片来自 OpenStreetMap |
| F-105 | 支持高德地图图源 | P1 | 用户填写 Key 和安全密钥后可切换高德图源 |
| F-106 | 高德坐标兼容 | P1 | 使用高德图源时，中国境内坐标按 GCJ-02 偏移显示 |
| F-107 | 地图视野自适应 | P0 | 加载、筛选、切换视图后地图自动缩放到可见足迹范围 |

### 6.3 足迹点与轨迹线

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| F-201 | 渲染足迹点 | P0 | 有效 CSV 行以点形式显示在地图上 |
| F-202 | 渲染轨迹线 | P0 | 按时间排序后的有效点形成轨迹线 |
| F-203 | 自动断开远距离轨迹 | P1 | 相邻点间隔超过 10 小时或距离超过 280 km 时默认断开 |
| F-204 | 可选连接远距离间隔 | P2 | 用户开启后，远距离间隔以额外线段连接 |
| F-205 | 支持点/线显示开关 | P1 | 用户可分别隐藏或显示足迹点和轨迹线 |
| F-206 | 支持点大小和线宽调整 | P2 | 用户可通过滑块调整，并可恢复默认值 |

### 6.4 时间筛选与列表联动

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| F-301 | 支持全局时间范围滑块 | P0 | 用户调整开始/结束滑块后，地图只显示对应范围内的数据 |
| F-302 | 支持年份列表 | P1 | 时间列表展示所有年份及点数，点击后筛选对应年份 |
| F-303 | 支持月份列表 | P1 | 选择年份后展示月份，点击后筛选对应月份 |
| F-304 | 支持日期列表 | P1 | 选择月份后展示日期，点击后筛选对应日期 |
| F-305 | 支持数据点列表 | P1 | 列表展示当前范围内的数据点时间、坐标、速度和海拔 |
| F-306 | 支持地图点与列表联动 | P1 | 点击地图点或列表项后高亮该点，并同步日期列表上下文 |
| F-307 | 支持大列表虚拟渲染 | P2 | 数据点列表只渲染可视窗口附近记录，避免大量 DOM 卡顿 |

### 6.5 统计信息

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| F-401 | 展示总点数 | P0 | 加载数据后显示有效点总数 |
| F-402 | 展示可见点数 | P0 | 筛选后显示当前可见点数量 |
| F-403 | 展示日期范围 | P0 | 显示数据最早和最晚日期 |
| F-404 | 展示总里程 | P1 | 优先使用 CSV `distance` 字段累加，缺失时按相邻点球面距离估算 |
| F-405 | 展示当前可见起止时间 | P1 | 时间滑块下方显示当前筛选起止时间 |

### 6.6 设置与本地存储

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| F-501 | 保存地图显示设置 | P1 | 地图模式、图源、远距离连接、点大小、线宽保存到 `localStorage` |
| F-502 | 保存高德凭据 | P1 | 高德 Key 和安全密钥保存到浏览器本地 `localStorage` |
| F-503 | 不上传 CSV 数据 | P0 | 应用不提供 CSV 上传接口，数据解析和渲染发生在浏览器本地 |
| F-504 | 忽略私人 CSV 提交 | P0 | `.gitignore` 默认忽略 `data/` 下除演示文件外的 CSV |

### 6.7 发布与运行

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| F-601 | 支持 Node.js 本地服务 | P0 | 运行 `serve.sh` 或 `serve.ps1` 后可访问 `http://127.0.0.1:5173/` |
| F-602 | 支持 Windows 便携启动器 | P1 | 便携版启动后在本机端口启动静态服务并打开浏览器 |
| F-603 | 支持内置演示资源 | P1 | 便携版缺少外部 `web` 文件夹时仍可读取内嵌网页和演示 CSV |

## 7. 数据需求

### 7.1 CSV 必需字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `dataTime` | Unix 秒或毫秒时间戳 | 足迹点发生时间 |
| `longitude` | 数字 | WGS84 经度 |
| `latitude` | 数字 | WGS84 纬度 |

### 7.2 CSV 可选字段

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `speed` | 数字 | 数据点列表展示速度 |
| `distance` | 数字，米 | 总里程统计优先使用 |
| `altitude` | 数字，米 | 数据点列表展示海拔 |
| `heading` | 数字 | 预留方向展示或后续增强 |
| `accuracy` | 数字 | 预留精度筛选或质量提示 |
| `locType`、`isBackForeground`、`stepType` | 任意 | 允许存在，当前版本不直接使用 |

### 7.3 数据处理规则

- CSV 支持 UTF-8 BOM，支持带引号字段和双引号转义。
- 数据按 `dataTime` 升序排序后渲染。
- 经纬度必须为有限数字，且经度在 `[-180, 180]`、纬度在 `[-90, 90]`。
- 时间必须可解析为有效 Unix 秒或毫秒时间戳。
- 无效行跳过，不中断整个文件加载。

## 8. 交互与界面要求

- 地图作为主视觉区域，占据首屏主体。
- 顶部提供全览、选择 CSV、恢复默认演示、设置和状态提示。
- 左侧控制面板展示数据源、统计信息、时间范围滑块和时间列表入口。
- 时间列表展开后展示年、月、日三列和数据点列表。
- 设置菜单应保持轻量，避免遮挡主要地图操作。
- 高德 Key 弹窗需提供输入、保存、关闭和获取方式说明。
- 移动端或窄屏下，控制面板和时间列表应适配为更紧凑布局。

## 9. 隐私与安全要求

- 用户 CSV 不上传到远端服务。
- 上次 CSV 仅保存到当前浏览器 IndexedDB。
- 高德 Key 和安全密钥仅保存到当前浏览器 `localStorage`。
- 仓库默认忽略本地私人 CSV、构建产物和发布包。
- 使用在线地图瓦片时，地图服务方仍可能收到瓦片请求和视野相关网络信息；产品界面或文档需避免暗示完全离线。

## 10. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 6 万级演示点应可加载、筛选和列表浏览；数据点列表使用虚拟渲染降低卡顿 |
| 可用性 | 首次打开无需配置即可查看演示数据 |
| 兼容性 | 支持现代桌面浏览器；本地服务依赖 Node.js；便携启动器面向 Windows |
| 可靠性 | 地图库、CSV、图源切换或浏览器存储失败时应给出清晰状态提示 |
| 可维护性 | 前端保持静态资源结构，核心逻辑集中在 `app.js`，样式集中在 `styles.css` |
| 可访问性 | 关键区域和控件保留基础 `aria-label`、按钮语义和键盘关闭弹窗能力 |

## 11. 成功指标

- 首屏默认演示数据加载成功率。
- 用户选择 CSV 后成功渲染率。
- 6 万级点数据加载到可交互的耗时。
- 筛选、点击数据点、切换地图模式的交互延迟。
- 用户因字段缺失或无效行导致的失败率。
- 私人 CSV 被误提交到仓库的次数保持为 0。

## 12. 里程碑建议

### v0.1 已具备能力

- 本地静态 Web 运行。
- 默认演示 CSV。
- CSV 选择和拖拽加载。
- MapLibre 地图、OpenStreetMap 图源、3D/平面模式。
- 高德图源配置。
- 足迹点、轨迹线、断点处理。
- 年/月/日/滑块筛选。
- 数据点列表联动。
- Windows 便携启动器。

### v0.2 建议方向

- 增加 CSV 字段映射配置，兼容不同导出格式。
- 增加轨迹质量过滤，例如按精度、速度异常、点间跳跃过滤。
- 增加离线瓦片或本地地图包说明，满足更高隐私场景。
- 增加 GPX、GeoJSON 导入。
- 增加按行程自动分段、停留点识别和摘要统计。

### v0.3 建议方向

- 增加导出当前视图截图或报告。
- 增加轨迹颜色规则，例如按速度、海拔、日期渐变。
- 增加多文件对比或多数据源合并。
- 增加隐私模式提示和本地数据清除入口。

## 13. 风险与依赖

| 风险/依赖 | 影响 | 应对 |
| --- | --- | --- |
| 在线地图瓦片不可用或限流 | 地图底图无法加载 | 保留图源切换；后续考虑离线瓦片 |
| 高德 Key 配置错误 | 高德图源不可用 | 在弹窗中保留获取方式说明和错误提示 |
| CSV 格式差异大 | 用户导入失败 | 后续增加字段映射和样例下载 |
| 大体量 CSV 性能压力 | 解析、筛选或地图渲染卡顿 | 继续优化解析、采样、分层渲染和 Worker 化 |
| 私人位置数据敏感 | 用户误传或误共享 | 保持 `.gitignore` 策略，文档持续强调本地使用边界 |

## 14. 待确认问题

- 是否需要把“完全离线使用”作为明确目标；若需要，需要补充离线地图瓦片方案。
- 是否需要支持更多坐标系和导出格式，例如 GCJ-02 原始 CSV、BD-09、GPX。
- 是否需要增加一键清除浏览器本地 CSV、设置和高德凭据。
- 是否需要把便携启动器作为主要交付物，还是仅作为演示版补充。
